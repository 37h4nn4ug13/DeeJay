{
  "name": "deejay",
  "version": "0.1.0",
  "description": "Electron + TypeScript DJ shell with native engine bindings",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "postbuild": "cp -r src/renderer dist/renderer",
    "start": "npm run build && electron dist/main.js"
  },
  "dependencies": {
    "electron": "^30.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.17",
    "typescript": "^5.4.0"
  }
}
